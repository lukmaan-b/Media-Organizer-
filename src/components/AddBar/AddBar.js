import React from 'react';

const AddBar = () => {
  return (
    <form>
      <input type="text" />
      <button>Games</button>
      <button>Add</button>
    </form>
  );
};

export default AddBar;
